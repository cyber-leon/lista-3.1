1/*Questão 24 Prepare uma função que receba um literal em um array e o rearrange de trás para frente
*/

#include <stdio.h>
#include <string.h>

int main(void) {

char  frase[100],i=0,x;

  puts("Digite uma frase \n\n");
   gets(frase);

 x= strlen (frase);
  
puts(frase);

 for(i=x;i>=0;i--){

	printf("\n%c",frase[i]);
}

  return 0;
}
